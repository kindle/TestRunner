<UserControl x:Class="TestViewer.Views.TestResultsDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vc="clr-namespace:TestViewer.ValueConverters"
             mc:Ignorable="d">
    <ScrollViewer>
        <ScrollViewer.Resources>
            <vc:StringToBitmapImageValueConverter x:Key="StringToBitmapImageConverter"></vc:StringToBitmapImageValueConverter>
            <vc:StringToVisibilityValueConverter x:Key="StringToVisibilityConverter"></vc:StringToVisibilityValueConverter>
        </ScrollViewer.Resources>
        <StackPanel>
            <Expander IsExpanded="True">
                <Expander.Header>
                    <TextBlock Text="Common Results" FontWeight="Bold"></TextBlock>
                </Expander.Header>
                <StackPanel Margin="20,5,0,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Width="120">Test Name:</TextBlock>
                        <TextBox Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" Text="{Binding Name}"></TextBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Width="120">Result:</TextBlock>
                        <Image Height="15" Source="{Binding State, Converter={StaticResource StringToBitmapImageConverter}}" Margin="0, 0, 5, 0"></Image>
                        <TextBox Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" Text="{Binding State}"></TextBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Width="120">Duration:</TextBlock>
                        <TextBox Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" Text="{Binding Duration, StringFormat={}{0:mm:ss.fff}}"></TextBox>
                    </StackPanel>
                </StackPanel>
            </Expander>
            <Expander IsExpanded="True" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                <Expander.Header>
                    <TextBlock FontWeight="Bold">Error Message</TextBlock>
                </Expander.Header>
                <StackPanel Margin="20,5,0,5">
                    <TextBox Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" Text="{Binding ErrorMessage}"></TextBox>
                </StackPanel>
            </Expander>
            <Expander IsExpanded="True" Visibility="{Binding StdOut, Converter={StaticResource StringToVisibilityConverter}}">
                <Expander.Header>
                    <TextBlock FontWeight="Bold">Standard Console Output</TextBlock>
                </Expander.Header>
                <StackPanel Margin="20,5,0,5">
                    <TextBox Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" Text="{Binding StdOut}"></TextBox>
                </StackPanel>
            </Expander>
            <Expander IsExpanded="True" Visibility="{Binding StackTrace, Converter={StaticResource StringToVisibilityConverter}}">
                <Expander.Header>
                    <TextBlock FontWeight="Bold">Error Stack Trace</TextBlock>
                </Expander.Header>
                <StackPanel Margin="20,5,0,5">
                    <TextBox Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" Text="{Binding StackTrace}"></TextBox>
                </StackPanel>
            </Expander>
            <Expander IsExpanded="True" Visibility="{Binding ResultFiles, Converter={StaticResource StringToVisibilityConverter}}">
                <Expander.Header>
                    <TextBox Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" FontWeight="Bold">Collected Files</TextBox>
                </Expander.Header>
                <StackPanel Margin="20,5,0,5">
                    <TextBlock>
                        <Hyperlink NavigateUri="{Binding ResultFiles}">
                            <TextBlock Text="{Binding ResultFiles}"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>
