<UserControl x:Class="TestViewer.Views.FolderBrowserBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="root">
    <DockPanel>
        <Button x:Name="theButton" DockPanel.Dock="Right" Click="theButtonClick" Padding="5,0,5,0">_Browse...</Button>
        <TextBox x:Name="theTextBox" Margin="0,0,2,0" MinWidth="{Binding ActualWidth, ElementName=theButton}">
            <TextBox.Text>
                <Binding Path="FolderName" ElementName="root" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
            </TextBox.Text>
        </TextBox>
    </DockPanel>
</UserControl>
